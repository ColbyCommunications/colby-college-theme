<component
  is="endpoint-filter"
  type="{{ type|default('light') }}"
  {% if initialEndpoint is defined %}:initial-endpoint="{{ initial_endpoint }}"{% endif %}
  {% if limit is defined %}:limit="{{ limit }}"{% endif %}
  {% if filters %}{% else %}:filters="false"{% endif %}
>
</component>